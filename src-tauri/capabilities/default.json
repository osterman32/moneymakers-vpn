{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — invoke commands, open URLs.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
